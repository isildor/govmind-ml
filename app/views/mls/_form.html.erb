<%= form_for(@ml) do |f| %>
  <% if @ml.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ml.errors.count, "error") %> prohibited this ml from being saved:</h2>

      <ul>
      <% @ml.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
