<%= form_for(@machine_learning) do |f| %>
  <% if @machine_learning.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@machine_learning.errors.count, "error") %> prohibited this machine_learning from being saved:</h2>

      <ul>
      <% @machine_learning.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
